"""Chat router.

Phase 1 stub. Implemented in Phase 2 (first end-to-end `/chat` endpoint) and
extended in Phase 5 (Planner) to route through the planner -> tools -> safety
gate flow instead of talking to the LLM directly.
"""

from __future__ import annotations
