#!/usr/bin/env bash
# Phase 1 stub. Implemented in Phase 2.
#
# Intended responsibility once implemented:
#   - Verify `uv`, `ollama`, and `brew` are installed
#   - Run `uv sync` inside backend/ to create the venv and install dependencies
#   - Call scripts/install_models.sh to pull required local models
#   - Install Playwright browser binaries (`playwright install chromium`)
set -euo pipefail
echo "setup.sh is a Phase 1 placeholder — implemented in Phase 2 (backend)." >&2
exit 1
